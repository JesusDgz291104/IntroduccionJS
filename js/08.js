//template strings
const client = "Job"
const balance =400

console.log(`The cliente : ${client} has a balance of ${balance}`)

const product = {
    name : "tablet",
    cost : 800,
    available : true
}

console.log(`The producto ${product.name} has a price of ${product.cost} and its availability is: ${product.available}`);
